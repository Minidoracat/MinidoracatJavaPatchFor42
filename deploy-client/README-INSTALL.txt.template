Minidoracat Client Patch __PATCH_VERSION__ — 實體隱形觀測＋修復（適用 Project Zomboid __GAME_VERSION__）
================================================================================

這個 patch 做什麼？
--------------------------------------------------------------------------------
針對「隊友/殭屍/車輛只剩影子和名牌、3D 模型看不到」的問題：
1.（根治，v2.0 起）修復遊戲本體的記憶體洩漏——動畫貼圖的每一幀在用完後
   從來不會被釋放，越玩越積、跑大城市時暴增，累積到上限後畫面上的新東西就
   永遠載不出來。修復後不再需要「玩幾小時就重開遊戲」。
   （額外紅利：部分模組的動畫貼圖原本會完全隱形，修復後會正常顯示。）
__LIMIT_SECTION__
3.（觀測）在遊戲 log（console.txt）記錄水位與 chunk 串流數據，用來驗證修復
   效果與鑑識黑邊事件。

若隱形仍出現，請把 %UserProfile%\Zomboid\console.txt 傳回來（裡面的數據能
告訴我們原因），並先用「完全關閉遊戲再重開」暫時恢復（斷線重連沒有用）。

安裝方式
--------------------------------------------------------------------------------
1. 完全關閉遊戲。
2. 把這個 zip 的「全部內容」解壓到任何資料夾（桌面、下載資料夾都可以）。
3. 雙擊「install.bat」。它會自動從 Steam 找到遊戲安裝位置並顯示出來，
   然後依序驗證：
   - 遊戲版本是否為 __GAME_VERSION__（檔案指紋比對，版本不符會拒絕安裝）
   - 是否已有其他 patch 佔用同位置（有衝突會拒絕安裝）
   全部通過才會複製檔案，並在複製後再次驗證檔案指紋。看到 [OK] 才算安裝完成。
4. 正常啟動遊戲即可。不影響 Steam、工作坊與其他玩家。

（若自動偵測失敗：把 zip 內容改解壓到遊戲資料夾——Steam 遊戲庫 →
  Project Zomboid 右鍵 → 管理 → 瀏覽本機檔案——再執行 install.bat 即可。）

怎麼確認 patch 有生效？
--------------------------------------------------------------------------------
進入伺服器玩一下之後，開啟 %UserProfile%\Zomboid\console.txt，
搜尋「TexPipelineGuard」，看到這一行就代表生效：
  [MinidoracatJavaPatch][TexPipelineGuard] active vanillaLimitBytes=52428800 effectiveLimitBytes=__EFFECTIVE_LIMIT__

log 裡「vanillaStallSamples=」的數字代表「原版遊戲在那個時刻會開始卡住等待」
的取樣次數；「aboveVanillaMs=」代表連續超標持續的毫秒數——這些就是問題
真的發生過的證據。「ChunkStream」開頭的行是黑邊鑑識數據（STALL 行的
notReadyAgoMs：有數字＝伺服器活著但地圖還沒生成好、-1＝完全斷流）。
回報時請把 console.txt 一起提供。

移除方式
--------------------------------------------------------------------------------
雙擊「uninstall.bat」（跟 install.bat 放在一起的那個，同樣會自動找到遊戲位置）。
它只會刪除本 patch 自己的檔案（__PAYLOAD_COUNT__ 個）（刪除前會先比對檔案指紋，
不是本 patch 的檔案一律不動）。

注意事項
--------------------------------------------------------------------------------
* 只適用遊戲版本 __GAME_VERSION__（install.bat 會自動擋版本不符）。
  遊戲更新版本後請先執行 uninstall.bat 移除，等新版本重新製作。
* 這是給受影響玩家的個人測試版，請勿再散布或上傳到工作坊。
* 本 patch 不改遊戲玩法、不碰網路封包，對伺服器與其他玩家完全無影響。
* 有任何異常（進不了遊戲、當機、電腦變很卡），執行 uninstall.bat 即可恢復原狀。
