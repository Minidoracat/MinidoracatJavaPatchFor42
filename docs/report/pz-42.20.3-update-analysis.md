# PZ 42.20.3 更新分析與 patch 存續判定（2026-08-17）

> 基準：`projectzomboid.jar` 42.20.2（sha256 前 8 碼 `09a80a46` 家族）→ 42.20.3
> （`bda809fb…`、buildid 24775771、version 字串 `42.20.3 70207f62e0`）。
> 方法：兩版 jar 全 entry CRC 對帳 → 我方 32 個相關 class 逐指令 javap 正規化 diff
> （濾常數池索引與 `ldc`/`ldc_w` 差異）→ 變更方法逐條語意比對 → 反編譯快照
> `42.20.3-20260817`（12,999 檔）語意對照。官方 changelog：
> <https://store.steampowered.com/news/app/108600/view/715661584899246561>

## 0. 事件時間軸

| 時刻（伺服器） | 事件 |
|---|---|
| 20:32 | cron `pzserver update` 自動換上 42.20.3 jar，48 個舊 loose class 殘留 |
| ~20:33 起 | 伺服器反覆啟動失敗：`NoSuchFieldError: IsoChunk … loadGridSquare`（舊 patched IsoChunk 遮蔽新 jar；42.20.3 把該欄位改型 `CappedConcurrentQueue`）。爆點在 `IsoWorld.init`＝**未觸碰存檔，fail-fast 安全** |
| 20:53 | uninstall 48 class + 強殺重啟 → 原版 42.20.3 恢復運行（f:5 進主迴圈、零 linkage error） |
| 21:0x | javap 全面重驗 → 重建 42.20.3 基準 dist → 三道閘裝回，**等下次重啟生效** |

## 1. 官方 42.20.3 動了什麼（changelog × bytecode 實證）

全 jar 23,735 → 23,740 class：**新增 6、移除 1、變更 143**。四大主題：

### A. MP 玩家上限 254＋滿員時管理員可進（changelog「player limit handling」）

- `GameServer`：新增 delayed-disconnect 機制（`MainLoopDelayedDisconnectQ`、
  `addDelayedDisconnect`/`doDelayedDisconnect`/`isDelayedDisconnect`）、`getFreeSlot` 收為
  private、`receiveClientConnect` 重構；`UdpConnection`/`UdpEngine`/`QueuePacket` 配合。
- `LoginPacket.processServer`：client 版本不符時**提前 `return`**（舊版 log+踢人後繼續跑完
  登入流程＝bug）。

### B. client 卡「Loading Map」forever 修復（chunk 供給管線重構）——**與我方 W4-1 同戰場**

- `PlayerDownloadServer`（+6,335 bytes，9→16 方法）新增 pending 機制：
  - 新內部類 `QueuedRequest`／`PendingChunk`／`OutOfRangeRequest`＋新封包
    `ChunkNotReadyPacket`。
  - Worker 遇未生成 chunk → `queueUntilGenerated` 入 `queuedByWorker`
    （ConcurrentLinkedQueue）；主緒 `updatePendingChunks()` 消費：無效 chunk 回
    `sendNotRequired`、超範圍入 `outOfRangeRequests`（上限 1024）、有效入 `pendingChunks`
    （LinkedHashMap，上限 4096，超限／生成逾時 30s 直接回 `ChunkNotReady` 告知 client）。
    生成完成後組新 ccr 塞回 `ccrWaiting`。
  - **整個 server 端重試機制刪除**：`Chunk.retriesCount`、`MAX_CHUNK_SEND_TRIES`、
    `getRetryChunk()` 全移除（42.20.2 的「3 次重試放棄」不復存在）。
  - `isRequestCancelled`/`sendNotRequired` 介面從 Chunk 實例改 requestNumber（int）。
  - **WorkerThread 從此不再寫 `ccrWaiting`**（回填一律走 concurrent queue）。
- client 端 `WorldStreamer`（+355）配合收 `ChunkNotReady`；`GameClient`（+110）。

### C. 記憶體洩漏多因修復＋記憶體優化（changelog「memory leak」「memory optimisations」）

- 新增 `zombie/popman/PoolCaps`（`GameServer.main` 每 tick `updateServerCaps()`）與
  `zombie/util/CappedConcurrentQueue`（有上限的池佇列）。
- `IsoChunkMap.chunkStore` 與 `IsoChunk.loadGridSquare` 改型 `CappedConcurrentQueue`
  ——**這就是舊 loose class 起不來的直接原因**。
- `ObjectPool`（+633）/`Pool`/`ReferencedObjectPool` 重構；`IsoChunk` pool 家族
  （`discardSquares`→`unlinkSquares`、`resetForStore`、`doReuseGridsquares`、
  `getFromPool`、`LoadBrandNew`）；`WorldReuserThread`。
- `IsoObject`：`highlightColor[]`／`renderInfo[]` 由建構子預配改**懶初始化**
  （新 `getOrCreateHighlightColor`）——每物件省兩個陣列＋元素物件。
- `GenericSpriteRenderState`（−1,435）、`ModelManager`、`LightingJNI`（lighting 錯誤修復）。

### D. 快速移動時黑邊/灰塊減少（changelog 明言「additional causes 仍在調查」）

即 B 的效果面。**官方沒修的 additional cause 正是我方 W4-1 定罪的吞吐瓶頸**（見下節）。

### 其他

- `WorldMapVisited`/`WorldMapVisitedServer`/`WorldMapClient` 重構、`PlayerVisitedPacket`
  移除、`RequestDataPacket`（+898）——rejoin 世界地圖同步主題（玩家回報「rejoin 掉圖」
  42.20.3 後仍存在）。
- 動物：`AnimalCell`（+971）/`AnimalChunk`/`AnimalManagerWorker`/`AnimalZones`。
- `ServerMap.preupdate` 刪 cancelLoading 處理段（−567）；kahlua `StringLib` 微調。

## 2. 我方 29 個 patched class 存續判定

javap 正規化 diff：**26/32 相關 class 逐指令相同**；6 個有變更的逐方法判定如下。

| 刀 | 42.20.3 對其手術方法的影響 | 判定 |
|---|---|---|
| 抑噪 8（AnimationSet/SkinningBoneHierarchy/SpriteConfig/ItemPickInfo/NetworkZombieManager/PacketsCache/INetworkPacket/**sendToxicBuilding**） | 全部逐指令相同（GameServer 變更不在 sendToxicBuilding；其 21 個 `DebugLog.log` 呼叫點 census 照過） | **全數保留** |
| 行為 IsoAnimal 三常數＋updateLOS 預篩 | class 逐指令相同（官方動的是 AnimalCell 等管理層） | 保留 |
| W5 容器環守衛（ItemContainer） | class 逐指令相同 | 保留 |
| W6 地圖格載入捕手（IsoChunk.doLoadGridsquare） | 方法逐指令相同；`IsoObject.addToWorld` 逐指令相同；`GameServer.main` 例外攔截點結構不變（僅加 PoolCaps 呼叫與版本字串） | 保留 |
| W7 朝向暫存隔離（IsoGameCharacter） | class 逐指令相同 | 保留 |
| W8 chunk 寫入閘（IsoChunk.Save＋SaveLoadedTask.save） | 兩方法逐指令相同；SafeWrite「恰 5 呼叫點」census 照過 | 保留（永久保險絲） |
| W9 存檔管線隔離（SaveChunkThread/SaveLoadedTask/ClientChunkRequest 私有池） | `releaseChunk`/`getByteBuffer`/`releaseBuffer` 逐指令相同；`getChunk` 僅刪 `retriesCount=0` 重置（fresh-shell 等價性維持——欄位本身已被刪）；`LoaderThread.run` 僅 chunkStore 改型（不涉我方改道） | 保留 |
| W4-1 chunk 供給併包（PlayerDownloadServer） | 掛點方法結構不變（僅 isRequestCancelled 簽名重構）；`update()` ready 閘仍在、掛點仍在閘內、vanilla 去重仍緊接其後；**吞吐瓶頸未修**（`RequestZipListPacket` 逐位元相同＝每包仍 new ccr；`update()` 每 tick 仍只處理一個 ccr）；worker 不再寫 ccrWaiting＝互斥前提更寬鬆 | **保留＋觀測**（見下） |
| 觀測 LoginMetrics（LoginPacket ×3） | 唯一語意變更＝版本不符提前 return（TIS bug fix），三個 DB wrapper 錨點無涉 | 保留 |
| 觀測 JoinMetrics（ConnectPacket/ConnectCoopPacket/GameServer.receivePlayerConnect） | receivePlayerConnect 逐指令相同 | 保留 |
| 效能（EngineEntityManager/EntityBucket/IsoZombie 預篩/BaseVehicle couldSee/NetworkZombiePacker stagger/Zombie/Fall headGuard/IsoWindow/LootRespawn/CreatePlayerPacket） | 全部 class 逐指令相同 | 保留 |
| ItemWeightMemo（InventoryItem，observe） | class 逐指令相同 | 保留（維持 observe，不啟 on） |

### 需要行為觀測的一項：W4-1

TIS 的 pending 機制解決的是「**未生成** chunk 請求佔住管線＋client 盲等」；我方 W4-1 解決的
是「**已生成** chunk 的供給吞吐只有設計值 15%」。兩者正交、掛點相容（已逐指令驗證），但生效
後應觀測：

- `ChunkPacker` 週期 log 的 `mergedChunks`／`avgBatchX10`——若官方 pending 回填讓佇列形狀改變
  （例如回填 ccr 已接近滿），`packed` 比例會自然下降，屬預期而非故障。
- `anomalies` 必須恆 0；`overrunTicks` 比率維持 0.1x% 量級。
- 官方 `ChunkNotReady` 上線後 client 不再對未生成 chunk 盲等重發——42.20.2 觀測到的
  「pending≈240、周期性重發」形狀會消失，屬官方修復效果，勿誤判為 W4-1 失效。

### 唯一退場項

- **SmokeCheck 的 `retriesCount` 斷言**（W9 私有池「新殼 retriesCount=0」）——vanilla 欄位
  連同重試機制整個刪除，斷言目標消失，已改為只驗 `bb`（fresh-shell 等價命題的剩餘部分）。
  helper 本體零改動。

## 3. client patch（dist-client v2.1）——**全部失效，裝了的玩家必須先移除**

- `WorldStreamer` 在 42.20.3 有實質重構（配合 ChunkNotReady 協定）：舊 loose class 會遮蔽
  新版 → 與 server 端同款 linkage 錯誤或行為錯亂。**先移除、勿繼續用**。
- `TextureIDAssetManager`/`ImageData`/`TextureID` 三個 class 42.20.3 逐指令相同
  （50MB 門檻與洩漏都還在，官方 memory leak 修復沒碰 texture pipeline 這條）。
- W4-2（`resendTimedOutRequests` 8000L→15000L）**必要性待重評**：官方 `ChunkNotReady`
  讓 server 主動告知「chunk 沒好」，client 盲等逾時的場景縮小；且 WorldStreamer 重構後
  錨點需重驗。重建 client patch 前逐錨點重跑 javap。

## 4. 部署狀態（2026-08-17 21:1x）

- 伺服器已裝回 48 class（三道閘全過：payload SHA ✓、29 個原版 class 與 42.20.3 jar 同源 ✓、
  無衝突 ✓），**下次重啟生效**（排程 0/6/12/18 或 MOD 更新觸發）。
- 本地：`work/projectzomboid.jar`＝42.20.3 基準（舊檔留存 `projectzomboid-42.20.2.jar`）；
  反編譯快照 `snapshots/42.20.3-20260817`（12,999 檔）已建。
- build 10/10 全綠：64 命中點、LoadCheck、BytecodeVerify、SmokeCheck 全部 census 與語境
  指紋、行為測試（W5/W4-1/W6/W9-off/記憶化三模式）、install-roundtrip 兩輪。
- 待辦：SmokeCheck/註解變更 commit 後 clean rebuild 換裝（消 dirty 指紋）；生效後跑
  docs/patches.md 第 3 節驗證清單。

## 5. 生效後驗證清單（docs/patches.md 第 3 節＋本次特化）

1. console 零 `VerifyError`/`NoSuchFieldError`/`NoClassDefFoundError`。
2. `PatchInfo` 開機橫幅：side=server、jar 指紋=`bda809fb`、build 時間對得上。
3. 抑噪生效：SkinningBoneHierarchy 等 WARN 消失、`Send Toxic Building` 為 0。
4. `ChunkPacker`／`ChunkWriteGuard`（`flagged=0`）／`AnimalSpotted` 週期 log 正常、
   全 patch `anomalies=0`。
5. 主迴圈 fps ~10、玩家 join 正常。
